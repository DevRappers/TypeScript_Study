{
  // target : 컴파일된 코드가 어떤환경에서 실행될 지 정의함.
  // module : 컴파일된 코드가 어떤 모듈 시스템을 사용할지 정의함.
  // strict : 모든 타입 체킹 옵션을 활성화한다는 것을 의미함.
  // esModuleInterop : commonjs 모듈 형태로 이루어진 파일을 es2015 모듈 형태로 불러올 수 있게 해줌.
  // outDir : 컴파일된 파일들이 저장되는 경로를 지정할 수 있음.
  "compilerOptions": {
    "target": "es5",
    "module": "commonjs",
    "strict": true,
    "esModuleInterop": true,
    "outDir": "./dist"
  }
}